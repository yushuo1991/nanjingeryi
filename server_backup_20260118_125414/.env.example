# Server port
PORT=3001

# Data storage (recommended to keep outside releases)
# Example: DB_PATH=/opt/ey-hospital/shared/db.json
# DB_PATH=

# CORS origin (dev). In production, prefer same-origin reverse proxy via Nginx.
# CORS_ORIGIN=http://localhost:3000
