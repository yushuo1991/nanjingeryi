server {
  listen 80;
  server_name ey.yushuo.click;

  root /opt/ey-hospital/current/dist;
  index index.html;

  # If you have SSL configured, consider redirecting HTTP to HTTPS.
  # return 301 https://$host$request_uri;

  location / {
    try_files $uri $uri/ /index.html;
  }

  location /api/ {
    proxy_pass http://127.0.0.1:3101/api/;
    proxy_http_version 1.1;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
  }
}

# Optional HTTPS server block (enable after installing certs).
# In BT panel, cert paths are usually:
# /www/server/panel/vhost/cert/ey.yushuo.click/fullchain.pem
# /www/server/panel/vhost/cert/ey.yushuo.click/privkey.pem
#
# server {
#   listen 443 ssl http2;
#   server_name ey.yushuo.click;
#
#   ssl_certificate /www/server/panel/vhost/cert/ey.yushuo.click/fullchain.pem;
#   ssl_certificate_key /www/server/panel/vhost/cert/ey.yushuo.click/privkey.pem;
#
#   root /opt/ey-hospital/current/dist;
#   index index.html;
#
#   location / {
#     try_files $uri $uri/ /index.html;
#   }
#
#   location /api/ {
#     proxy_pass http://127.0.0.1:3101/api/;
#     proxy_http_version 1.1;
#     proxy_set_header Host $host;
#     proxy_set_header X-Real-IP $remote_addr;
#     proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#     proxy_set_header X-Forwarded-Proto $scheme;
#   }
# }
